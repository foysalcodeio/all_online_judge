using stl - string to binary
 - cout << bitset<8>(x) << endl;

int powerOfTwo(int x){
    return (x&&!(x&(x-1)));
}

//num to binary
cout<<bitset<const_length>(number);


for(int i=0; i<30; i++){
    if((x&(1<<i))!=0){
       cout << i << " ";
   }
 }
    //better way
 for(int i=0; (1<<i)<=x; i++){
      if(x&(1<<i)){
          cout << i << " ";
      }
  }
